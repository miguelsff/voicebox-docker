# GPU selection (default: GPU 0)
# Set to "all" to use all GPUs, or a comma-separated list (e.g., "0,1")
CUDA_VISIBLE_DEVICES=0

# HuggingFace cache directory inside the container (do not change unless you know what you're doing)
HF_HOME=/root/.cache/huggingface
